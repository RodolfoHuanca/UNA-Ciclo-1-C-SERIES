#include <iostream>
using namespace std;
int main() {
    int N;
    cin >> N;
    int x = 1;
    for (int i = 0; i < N; i++) {
        cout << x << " ";
        if (i % 2 == 0) {
            x += 1;
        } else {
            x += 2;
        }
    }
    return 0;
}
