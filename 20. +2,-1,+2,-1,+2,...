#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;

    int x = 1;
    bool y = true;

    for (int i = 0; i < n; ++i) {
        cout << x << " ";

        if (y) {
            x=x+2;
        } else {
            x=x-1;
        }
        y = !y;
    }
}
